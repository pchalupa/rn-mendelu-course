import { StyleSheet } from 'react-native';

import { colors, font } from '~/constants/theme';

export const styles = StyleSheet.create({
  container: { fontFamily: font.sans, color: colors.primary },
});
